<h1>Stats for <%= @user.uri %></h1>

<table>
	<thead>
		<tr>
			<th>Year</th>
			<th>Count</th>
		</tr>
	</thead>
	<tbody>
		<% @year_labels.zip(@year_values).each do |year, count| %>
			<tr>
				<td><%= year %></td>
				<td><%= count %></td>
			</tr>
		<% end %>
	</tbody>
</table>